#!/usr/bin/env python3
"""Lanceur pour exécution depuis un clone : `python3 generate.py [options]`.
Équivalent à la commande `wrapped-claude` une fois le paquet installé."""
from wrapped_claude.cli import main

if __name__ == "__main__":
    main()
